return {
  -- This file now only adds plugins that are NOT part of the dap.core extra.
  {
    "theHamsta/nvim-dap-virtual-text",
    -- We can add opts to configure it if needed
    opts = {},
  },
}
